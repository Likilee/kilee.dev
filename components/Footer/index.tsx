import cn from "classnames"

export default function Footer() {
  return (
    <footer
      className={cn(
        'bg-gray-50 dark:bg-gray-900',
        'max-w-2xl mx-auto',
      )}
    >
      <div>Footer</div>
    </footer>
  )
}
